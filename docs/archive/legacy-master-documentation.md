# Legacy Master Documentation

This file replaces the old root-level `MASTER_DOCUMENTATION.md` as an archived reference.

## Context

The earlier master document described a broader version of the project architecture and setup. It is still useful for historical context, but parts of it predate the current orchestration-based learning runtime.

## Topics It Covered

- high-level product overview
- frontend and backend architecture
- API surface
- Supabase data model
- environment setup

## Current Replacement Docs

For the current runtime, use these documents instead:

- `docs/agent-runtime-overview.md`
- `docs/initial-quiz-and-placement.md`
- `docs/backend/roadmap-agent.md`

This archive entry exists so the older document can be removed from the repo root without losing its intent.
